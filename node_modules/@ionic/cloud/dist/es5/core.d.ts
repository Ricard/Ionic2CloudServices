import { CoreDependencies, ICore, ISettings } from './definitions';
export declare class Core implements ICore {
    cfg: ISettings;
    private config;
    private logger;
    private emitter;
    private insights;
    private _version;
    constructor(deps: CoreDependencies, cfg?: ISettings);
    version: string;
    private registerEventHandlers();
}
