import { ISettings, IConfig } from './definitions';
export declare class Config implements IConfig {
    private settings;
    private urls;
    register(settings: ISettings): void;
    get(name: string): any;
    getURL(name: string): string;
}
