import { IPushToken, PushDependencies, PushOptions, IPush, SaveTokenOptions } from '../definitions';
export declare class Push implements IPush {
    plugin: any;
    private app;
    private config;
    private auth;
    private userService;
    private device;
    private client;
    private emitter;
    private storage;
    private logger;
    private blockRegistration;
    private blockUnregister;
    private blockSaveToken;
    private registered;
    private options;
    private _token;
    constructor(deps: PushDependencies, options?: PushOptions);
    token: IPushToken;
    saveToken(token: IPushToken, options?: SaveTokenOptions): Promise<IPushToken>;
    /**
     * Registers the device with GCM/APNS to get a device token
     */
    register(): Promise<IPushToken>;
    /**
     * Invalidate the current GCM/APNS token
     */
    unregister(): Promise<void>;
    /**
     * Registers callbacks with the PushPlugin
     */
    private _callbackRegistration();
    private _getPushPlugin();
}
