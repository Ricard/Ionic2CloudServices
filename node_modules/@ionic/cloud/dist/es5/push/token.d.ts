import { IPushToken } from '../definitions';
export declare class PushToken implements IPushToken {
    token: string;
    registered: boolean;
    saved: boolean;
    constructor(token: string);
    toString(): string;
}
