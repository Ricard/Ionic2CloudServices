import { CordovaDependencies, CordovaOptions, ICordova } from './definitions';
export declare class Cordova implements ICordova {
    protected options: CordovaOptions;
    private device;
    private emitter;
    private logger;
    constructor(deps: CordovaDependencies, options?: CordovaOptions);
    bootstrap(): void;
    private load();
    private isAvailable();
}
